# Lite Bundle Size — Webpack

Standard webpack production build.

## Run

```bash
npm install && npx webpack
```

## Expected output

- `build/bundle.min.js` — under 7 KB gzipped
- Zero encryption code, zero event-spec validation code
